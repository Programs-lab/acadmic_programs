<div class="flex flex-wrap justify-center my-12">
  <div class="flex flex-wrap md:shadow-md rounded px-6 py-6 bg-white w-full">
    <div class="flex flex-wrap w-full justify-center mt-1 mb-5 border-b border-grey-dark pb-3">
      <span class="">Iniciar Sesion</span>
    </div>
    <div class="flex w-full">
      <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: "w-full" } ) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>
        <div class="flex flex-wrap -mx-3 mb-7">
          <div class="w-full px-3 focus_form mb-3">
            <%= f.label :email, class: 'label_input' %>
            <%= f.email_field :email, autocomplete: "email", class: 'field_input rounded-t' %>
          </div>
          <div class="w-full px-3 focus_form">
            <%= f.label :contraseña, class: 'label_input' %>
            <%= f.password_field :password, class: 'field_input rounded-t' %>
          </div>
        </div>
        <div class="md:flex md:items-center justify-center mt-8">
          <%= f.submit "Registrar", class: "btn btn-primary w-full" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
