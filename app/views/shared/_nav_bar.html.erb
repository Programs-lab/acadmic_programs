<nav class="flex items-center flex-wrap shadow">
  <div class="container mx-auto flex">
    <div class="w-1/2 flex items-center m-0">
      <div class="w-48 flex-no-shrink text-white justify-start">
        <img src="/logo_complete.png" weight="25%" alt="">
      </div>
    </div>
    <% unless current_user %>
    <div class="w-1/2 flex items-center justify-end">
      <%= link_to("Registrarse", new_user_registration_path, class: "bg-primary text-white px-5 py-2 no-underline hover:bg-secondary rounded focus:outline-none" ) %>
    </div>
    <% else %>
    <div class="w-1/2 flex items-center justify-end">
      <%= link_to("Cerrar session", destroy_user_session_path, method: :delete, class: "bg-primary text-white px-5 py-2 no-underline hover:bg-secondary rounded focus:outline-none" ) %>
    </div>
    <% end %>
  </div>
</nav>
