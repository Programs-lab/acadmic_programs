<% if resource.errors.any? %>
  <div id="error_explanation">
    <% resource.errors.full_messages.each do |message| %>
      <%= render partial: 'shared/flash', locals: {flash: {"danger" => message}}%>
    <% end %>
  </div>
<% end %>
